import Link from "next/link"; import SeoBreadcrumbs from "../../../components/SeoBreadcrumbs"; import {notFound} from "next/navigation"; import ProductCard from "../../../components/ProductCard"; import AffiliateDisclosure from "../../../components/AffiliateDisclosure"; import {guides,getGuide,products} from "../../../lib/catalog";
export function generateStaticParams(){return guides.map(g=>({slug:g.slug}));}
export async function generateMetadata({params}){const {slug}=await params;const g=getGuide(slug);if(!g)return{};return{title:g.title,description:g.description,alternates:{canonical:`/guides/${g.slug}`},openGraph:{title:`${g.title} | Paw Nova`,description:g.description,url:`https://pawnovaco.com/guides/${g.slug}`}}}
export default async function Guide({params}){const {slug}=await params;const g=getGuide(slug);if(!g)notFound();const categoryBridge={"grooming":["grooming-and-hygiene"],"walking-travel":["travel-and-car","walking-and-outdoor"],"beds-comfort":["beds-and-comfort","crates-and-playpens"],"feeding":["feeding-and-hydration","dog-food","dog-treats","health-and-supplements"],"play-enrichment":["toys-and-enrichment","training"],"everyday-essentials":["pet-essentials","cleanup-and-home"]}; const related=[...new Set(g.relatedCategories.flatMap(c=>categoryBridge[c]||[]))]; const picks=products.filter(p=>related.includes(p.category)).slice(0,6);const schema={"@context":"https://schema.org","@type":"Article",headline:g.title,description:g.description,dateModified:g.updated,author:{"@type":"Organization",name:"Paw Nova"},publisher:{"@type":"Organization",name:"Paw Nova"},mainEntityOfPage:`https://pawnovaco.com/guides/${g.slug}`};return <><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/><section className="page-hero"><div className="container narrow"><SeoBreadcrumbs items={[{name:"Home",href:"/"},{name:"Guides",href:"/guides"},{name:g.title,href:`/guides/${g.slug}`}]}/><span className="eyebrow">{g.cluster} Guide</span><h1>{g.title}</h1><p>{g.description}</p><AffiliateDisclosure compact/></div></section><section className="section"><div className="container narrow legal"><h2>Start with the problem, not the product</h2><p>The best choice depends on your dog's size, habits, environment, and the job you actually need the product to do. Compare fit, materials, cleaning, safety, and ease of use before focusing on brand names.</p><h2>What Paw Nova looks for</h2><p>We favor practical designs, clear use cases, sensible maintenance, and products that solve a recognizable dog-owner problem. We also call out tradeoffs instead of pretending one option is right for every dog.</p><h2>Use recommendations as a shortlist</h2><p>Our picks are a starting point for your own decision. Product details can change, and individual dogs have different needs. For health or safety questions specific to your dog, use qualified professional advice rather than a shopping guide.</p></div></section><section className="section section-soft"><div className="container"><div className="section-heading"><div><span className="eyebrow">Related picks</span><h2>Products worth comparing</h2></div></div><div className="cards-grid">{picks.map(p=><ProductCard key={p.slug} product={p}/>)}</div><p className="center-text"><Link href="/products" className="button button-secondary">Browse all products</Link></p></div></section></>;}
