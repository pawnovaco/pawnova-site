import ProductCard from "./ProductCard"; import AffiliateDisclosure from "./AffiliateDisclosure"; import {productsForCategory} from "../lib/catalog";
export default function CategoryPage({eyebrow,title,intro,category}){const items=productsForCategory(category);return <><section className="page-hero"><div className="container narrow"><span className="eyebrow">{eyebrow}</span><h1>{title}</h1><p>{intro}</p><AffiliateDisclosure compact/></div></section><section className="section"><div className="container"><div className="catalog-toolbar"><p><strong>{items.length} practical product types</strong> to research and compare.</p><p className="muted">Product links use the reviewed Amazon destination supplied for each find.</p></div><div className="cards-grid">{items.map(p=><ProductCard key={p.slug} product={p}/>)}</div></div></section><section className="section section-soft"><div className="container narrow"><h2>How we choose what to feature</h2><p>Paw Nova organizes recommendations around real pet-owner jobs and problems. We compare usefulness, fit, maintenance, materials, safety considerations, and value before adding a specific affiliate destination.</p></div></section></>;}
